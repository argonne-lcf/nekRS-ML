module restore
module swap PrgEnv-nvhpc PrgEnv-gnu
module load cudatoolkit-standalone
module load cmake
#module load nvhpc-mixed/21.9
#module unload cray-libsci

export CRAY_ACCEL_TARGET=nvidia80
export MPICH_GPU_SUPPORT_ENABLED=1

echo $(cc --version)
echo $(CC --version)
echo $(ftn --version)
echo $(gcc --version)
echo $(g++ --version)
echo $(gfortran --version)

export NEKRS_HOME=$PWD/install_dir/nekrs
export PATH=$NEKRS_HOME/bin:$PATH
 
export NEK5000_HOME=/lus/eagle/projects/datascience/sbarwey/codes/nek/Nek5000
export PATH=$NEK5000_HOME/bin:$PATH
