# @package _global_
verbose: True
seed: 12
backend: nccl
lr_init: 0.00001

# arch properties 
hidden_channels: 32 
n_mlp_hidden_layers: 2 
n_messagePassing_layers: 4

# Halo swap mode 
halo_swap_mode : none

hydra:
  job:
    chdir: true

    #defaults:
    #  - override hydra/hydra_logging: colorlog
    #  - override hydra/job_logging: colorlog

# path to original working directory
# hydra hijacks working directory by changing it to the new log directory
# so its useful to have this path as a special variable
# https://hydra.cc/docs/next/tutorials/basic/running_your_app/working_directory
work_dir: ${hydra:runtime.cwd}
# path to folder with data
data_dir: ${work_dir}/datasets/
# path to folder for checkpointing
ckpt_dir: ${work_dir}/ckpt/ 
# path to saved model directory
model_dir: ${work_dir}/saved_models/ 


# specify path to gnn_outputs here (case directory)  
gnn_outputs_path: /lus/eagle/projects/datascience/sbarwey/codes/nek/nekrs_cases/examples_v23_gnn/tgv/gnn_outputs_poly_1/
